CAM_SSID="FZ80-9D2C1C"

if [ "$ifssid" = "$CAM_SSID" ] && [ "$reason" = BOUND ]
then
    curl "http://192.168.54.1/cam.cgi?mode=accctrl&type=req_acc&value=0&value2=surfptz"
    curl "http://192.168.54.1/cam.cgi?mode=camcmd&value=wide-fast"
fi
